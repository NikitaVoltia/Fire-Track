/** @type {import('next').NextConfig} */
const nextConfig = {
  typescript: {
    ignoreBuildErrors: true,
  },
  images: {
    unoptimized: true,
    /** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  },
}

export default nextConfig
